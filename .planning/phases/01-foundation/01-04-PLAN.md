---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - Makefile
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - BLDG-01
  - BLDG-02
  - BLDG-03
  - BLDG-04
  - BLDG-05
  - BLDG-06

must_haves:
  truths:
    - "`make lint` runs cargo clippy with -D warnings and exits 0 on clean code"
    - "`make fmt` runs cargo fmt --check and exits 0 on properly formatted code"
    - "`make test` runs cargo test and exits 0 with all tests passing"
    - "`make build` builds both debug and release targets and exits 0"
    - "GitHub Actions CI workflow file exists and is valid YAML"
    - "CI workflow runs lint, fmt, test, and musl release build steps"
    - "CI workflow includes tree-sitter duplicate ABI check"
    - "CI workflow includes binary size assertion (< 15MB)"
  artifacts:
    - path: "Makefile"
      provides: "lint, fmt, test, build targets"
      contains: "cargo clippy -- -D warnings"
    - path: ".github/workflows/ci.yml"
      provides: "Full CI pipeline: lint + fmt + test + musl build + size check"
      contains: "x86_64-unknown-linux-musl"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "Cargo.toml"
      via: "cargo commands in workflow steps"
      pattern: "cargo (clippy|fmt|test|build)"
---

<objective>
Write the Makefile and GitHub Actions CI workflow that gate all future work.

Purpose: After this plan, `make lint && make fmt && make test && make build` must all pass. The CI pipeline enforces the same checks on every push. The musl binary size assertion in CI prevents binary bloat from being discovered post-Phase 4.
Output: Makefile with 4 targets; .github/workflows/ci.yml with lint/fmt/test/musl-build/size-check steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Key build commands confirmed in RESEARCH.md Patterns 7 and 8 -->

From 01-RESEARCH.md Pattern 7 (Makefile):
```makefile
.PHONY: lint fmt test build

lint:
    cargo clippy -- -D warnings

fmt:
    cargo fmt --check

test:
    cargo test

build:
    cargo build
    cargo build --release
```

From 01-RESEARCH.md Pattern 8 (GitHub Actions):
```yaml
- uses: dtolnay/rust-toolchain@stable
  with:
    toolchain: stable
    targets: x86_64-unknown-linux-musl
    components: clippy, rustfmt

- name: Lint
  run: cargo clippy -- -D warnings

- name: Format check
  run: cargo fmt --check

- name: Test
  run: cargo test

- name: Build musl release binary
  run: cargo build --release --target x86_64-unknown-linux-musl

- name: Check tree-sitter no duplicates
  run: cargo tree --duplicates | grep tree-sitter && exit 1 || exit 0

- name: Assert binary under 15MB
  run: |
    SIZE=$(wc -c < target/x86_64-unknown-linux-musl/release/arcanon-scanner)
    [ "$SIZE" -lt 15728640 ] || (echo "Binary too large: $SIZE bytes" && exit 1)
```

Note from PITFALLS.md Pitfall 10:
Binary size target: 15MB = 15,728,640 bytes. Use `wc -c` for byte count (not `ls -l`).
musl-tools needed on ubuntu-latest: `sudo apt-get install -y musl-tools`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Makefile with lint, fmt, test, build targets</name>
  <files>Makefile</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 7: Makefile structure)
    - .planning/REQUIREMENTS.md (BLDG-01 through BLDG-04)
  </read_first>
  <action>
Create `Makefile` at the repo root. CRITICAL: Makefile recipes MUST use actual tab characters (not spaces). If you use spaces, `make` will fail with "missing separator".

```makefile
.PHONY: lint fmt test build

## Run clippy with denied warnings (BLDG-01)
lint:
	cargo clippy -- -D warnings

## Check code formatting (BLDG-02)
fmt:
	cargo fmt --check

## Run all tests (BLDG-03)
test:
	cargo test

## Build debug and release binaries (BLDG-04)
build:
	cargo build
	cargo build --release
```

Note: The indentation before each `cargo` command in the recipes MUST be a tab character (ASCII 0x09), not spaces. This is a hard Make requirement.

After writing the file, verify each target runs successfully:
- `make lint` — should exit 0 (clippy passes on clean code)
- `make fmt` — should exit 0 (code should already be formatted; if not, run `cargo fmt` first then check)
- `make test` — should exit 0 (all 9 CLI tests from Plan 03 pass)
- `make build` — should exit 0 (debug and release both build)

If `make fmt` fails because the code isn't formatted, run `cargo fmt` first to format the code, then verify `make fmt` passes.
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/arcanon-scanner && make lint 2>&1 | tail -3; echo "lint_exit:$?"; make fmt 2>&1 | tail -3; echo "fmt_exit:$?"; make test 2>&1 | tail -5; echo "test_exit:$?"; make build 2>&1 | tail -3; echo "build_exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `make lint` exits 0
    - `make fmt` exits 0
    - `make test` exits 0
    - `make build` exits 0 (both debug and release binaries produced)
    - `grep "cargo clippy -- -D warnings" Makefile` returns a match
    - `grep "cargo fmt --check" Makefile` returns a match
    - `file Makefile` shows it exists (basic sanity)
    - `cat -A Makefile | grep "^\^I"` shows tab characters on recipe lines (not spaces)
  </acceptance_criteria>
  <done>Makefile written; all four targets (lint, fmt, test, build) pass</done>
</task>

<task type="auto">
  <name>Task 2: Write GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 8: GitHub Actions CI)
    - .planning/research/PITFALLS.md (Pitfall 10: binary size assertion; Pitfall 1: tree-sitter duplicate check)
    - .planning/REQUIREMENTS.md (BLDG-05, BLDG-06)
  </read_first>
  <action>
Create `.github/workflows/ci.yml`. The `.github/workflows/` directory must be created if it doesn't exist.

Write the full CI workflow with these exact requirements:
- Trigger: `on: [push, pull_request]` (BLDG-05: runs on push/PR)
- Runner: `ubuntu-latest`
- Install musl-tools via apt before the build step (required for musl target on ubuntu-latest)
- Use `dtolnay/rust-toolchain@stable` with `targets: x86_64-unknown-linux-musl` and `components: clippy, rustfmt`
- Use `actions/checkout@v4`

Steps in order:
1. Checkout
2. Install Rust toolchain (with musl target + clippy + rustfmt)
3. Install musl linker: `sudo apt-get install -y musl-tools`
4. Cache: use `actions/cache@v4` for `~/.cargo/registry`, `~/.cargo/git`, `target/` keyed on `Cargo.lock` hash
5. Lint: `cargo clippy -- -D warnings`
6. Format check: `cargo fmt --check`
7. Test: `cargo test`
8. Build musl release binary: `cargo build --release --target x86_64-unknown-linux-musl` (BLDG-06)
9. Check tree-sitter no duplicates: `cargo tree --duplicates | grep tree-sitter && exit 1 || exit 0`
10. Assert binary under 15MB: shell script using `wc -c`

The complete workflow file:

```yaml
name: CI

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: x86_64-unknown-linux-musl
          components: clippy, rustfmt

      - name: Install musl linker
        run: sudo apt-get install -y musl-tools

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Lint
        run: cargo clippy -- -D warnings

      - name: Format check
        run: cargo fmt --check

      - name: Test
        run: cargo test

      - name: Build musl release binary
        run: cargo build --release --target x86_64-unknown-linux-musl

      - name: Check tree-sitter ABI — no duplicates allowed
        run: |
          if cargo tree --duplicates | grep tree-sitter; then
            echo "ERROR: duplicate tree-sitter versions detected — ABI mismatch risk"
            exit 1
          fi
          echo "OK: no tree-sitter version duplicates"

      - name: Assert binary size under 15MB
        run: |
          BINARY="target/x86_64-unknown-linux-musl/release/arcanon-scanner"
          SIZE=$(wc -c < "$BINARY")
          LIMIT=15728640
          echo "Binary size: $SIZE bytes (limit: $LIMIT bytes)"
          if [ "$SIZE" -ge "$LIMIT" ]; then
            echo "ERROR: binary too large ($SIZE >= $LIMIT bytes)"
            exit 1
          fi
          echo "OK: binary within size limit"
```

After writing the file, verify the YAML is syntactically valid:
`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML valid"`

Also confirm the tree-sitter duplicate check logic is correct: the step should exit 1 if `grep tree-sitter` finds output (duplicates present), and exit 0 if no output (clean).
  </action>
  <verify>
    <automated>cd /Users/ravichillerega/sources/arcanon-scanner && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML valid')" 2>&1; echo "---content-checks---"; grep -c "cargo clippy" .github/workflows/ci.yml; grep -c "x86_64-unknown-linux-musl" .github/workflows/ci.yml; grep -c "15728640" .github/workflows/ci.yml; grep -c "tree-sitter" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - Python yaml.safe_load succeeds: output contains "YAML valid"
    - `grep "cargo clippy -- -D warnings" .github/workflows/ci.yml` returns a match
    - `grep "cargo fmt --check" .github/workflows/ci.yml` returns a match
    - `grep "cargo test" .github/workflows/ci.yml` returns a match
    - `grep "x86_64-unknown-linux-musl" .github/workflows/ci.yml` returns at least 2 matches (toolchain target + build command)
    - `grep "15728640" .github/workflows/ci.yml` returns a match (binary size limit in bytes)
    - `grep "tree-sitter" .github/workflows/ci.yml` returns a match (duplicate check step)
    - `grep "musl-tools" .github/workflows/ci.yml` returns a match (apt install step)
    - `grep "actions/cache@v4" .github/workflows/ci.yml` returns a match
  </acceptance_criteria>
  <done>GitHub Actions CI workflow written with all required steps; YAML is syntactically valid</done>
</task>

</tasks>

<verification>
After both tasks complete — the full Phase 1 success criteria check:

1. `make lint` exits 0 (BLDG-01)
2. `make fmt` exits 0 (BLDG-02)
3. `make test` exits 0 (BLDG-03)
4. `make build` exits 0 — produces both `target/debug/arcanon-scanner` and `target/release/arcanon-scanner` (BLDG-04)
5. `.github/workflows/ci.yml` is valid YAML containing all required job steps (BLDG-05, BLDG-06)
6. `./target/debug/arcanon-scanner --help` shows all 11 flag groups
7. `./target/debug/arcanon-scanner --version` prints version
8. `cargo tree --duplicates | grep tree-sitter` returns empty (no ABI conflict)
9. `grep "lto = \"fat\"" Cargo.toml` matches (BLDG-07 — already set in Plan 01)
</verification>

<success_criteria>
- All four Make targets pass: `make lint`, `make fmt`, `make test`, `make build`
- CI workflow is syntactically valid YAML with: lint, fmt, test, musl build, tree-sitter duplicate check, binary size assertion steps
- The binary size assertion in CI uses 15728640 bytes (15MB) as the limit
- musl-tools installed in CI before musl build step
- Cargo cache configured in CI with Cargo.lock-based cache key
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` following the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
