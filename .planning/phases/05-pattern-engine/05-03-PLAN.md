---
phase: 05-pattern-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/lang/typescript.rs
  - src/plugin/lang/python.rs
  - src/plugin/lang/go.rs
  - src/plugin/lang/java.rs
  - src/plugin/lang/csharp.rs
  - src/plugin/lang/rust_lang.rs
  - src/plugin/lang/ruby.rs
autonomous: true
requirements:
  - PTRN-05
  - PTRN-06

must_haves:
  truths:
    - "Each language plugin's extract() returns only route/endpoint findings — zero ConnectionInfo in connections field"
    - "No plugin file contains import_gate or line-scan connection detection code after stripping"
    - "Each plugin still returns correct ServiceInfo and EndpointInfo findings from AST queries"
    - "cargo test passes with all existing route-extraction tests still green"
  artifacts:
    - path: "src/plugin/lang/typescript.rs"
      provides: "Route extraction only (Express, NestJS, Next.js, Fastify)"
    - path: "src/plugin/lang/python.rs"
      provides: "Route extraction only (FastAPI, Django, Flask)"
    - path: "src/plugin/lang/go.rs"
      provides: "Route extraction only (net/http, Gin, Echo)"
    - path: "src/plugin/lang/java.rs"
      provides: "Route extraction only (Spring Boot two-phase)"
    - path: "src/plugin/lang/csharp.rs"
      provides: "Route extraction only (ASP.NET Core two-phase)"
    - path: "src/plugin/lang/rust_lang.rs"
      provides: "Route extraction only (Actix, Axum, Rocket)"
    - path: "src/plugin/lang/ruby.rs"
      provides: "Route extraction only (Rails resources expansion, Sinatra)"
  key_links:
    - from: "src/plugin/lang/*.rs"
      to: "ExtractionResult"
      via: "connections: vec![] in all returned ExtractionResult instances"
---

<objective>
Strip all content-gate + line-scan connection detection code from all 7 language plugins. After this plan, language plugins are route-extraction-only. Connection detection moves exclusively to the pattern engine (Plan 01). This implements D-05 and D-06.

Purpose: Clean separation of concerns — plugins own AST-based route extraction, patterns own all connection detection.
Output: 7 language plugin files with connection-detection code removed but all route-extraction code intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-pattern-engine/05-CONTEXT.md
@src/plugin/lang/typescript.rs
@src/plugin/lang/python.rs
@src/plugin/lang/go.rs
@src/plugin/lang/java.rs
@src/plugin/lang/csharp.rs
@src/plugin/lang/rust_lang.rs
@src/plugin/lang/ruby.rs
@src/types/mod.rs
</context>

<interfaces>
<!-- What stays vs what goes -->

STRIP (connection detection — moves to pattern engine):
- All functions that detect HTTP clients: fetch/axios/got, httpx/requests/aiohttp, http.Get, RestTemplate/WebClient, HttpClient, reqwest, Faraday/Net::HTTP
- All functions that detect MQ clients: kafkajs/amqplib/pika/sarama/rdkafka/rumqttc/Sidekiq
- All functions that detect DB clients: pg/mongoose/redis/sqlx/sequelize/ecto etc.
- All functions that detect gRPC clients: grpc.Dial, ServiceStub constructors, tonic::transport
- All functions that detect industrial protocols: pymodbus/opcua/bacpypes etc.
- All OnceLock/static Query caches for connection queries
- All const query strings for connection detection
- All `ConnectionInfo` constructions in extract()

KEEP (route extraction — stays in compiled plugins):
- Framework marker detection (package.json/go.mod/Gemfile/etc scanning)
- AST queries for HTTP route declarations
- Two-phase prefix joining (NestJS @Controller, Spring @RequestMapping, ASP.NET [Route])
- Rails resources expansion static table
- .NET Minimal API MapGet/MapPost line scanning
- ServiceInfo construction
- EndpointInfo construction
- scope_to_service calls for service attribution

The final extract() return value must be:
```rust
ExtractionResult {
    services: /* unchanged */,
    endpoints: /* unchanged */,
    connections: vec![],     // EMPTY — pattern engine handles this
    schemas: vec![],
    actors: vec![],
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Strip connection detection from TypeScript and Python plugins</name>
  <files>
    src/plugin/lang/typescript.rs,
    src/plugin/lang/python.rs
  </files>
  <read_first>
    - src/plugin/lang/typescript.rs — read fully before editing (2003 lines)
    - src/plugin/lang/python.rs — read fully before editing (1397 lines)
    - .planning/phases/05-pattern-engine/05-CONTEXT.md — D-05, D-06 decisions
  </read_first>
  <action>
TypeScript plugin (src/plugin/lang/typescript.rs):

REMOVE these elements completely:
- const FETCH_CALL_QUERY, GRPC_NEW_QUERY, MONGOOSE_CONNECT_QUERY, KAFKA_SEND_QUERY, HTTP_CLIENT_METHOD_QUERY, CONSTRUCTOR_CALL_QUERY
- static FETCH_QUERY_CACHE, GRPC_NEW_QUERY_CACHE, MONGOOSE_CONNECT_QUERY_CACHE, KAFKA_SEND_QUERY_CACHE, HTTP_CLIENT_METHOD_QUERY_CACHE, CONSTRUCTOR_CALL_QUERY_CACHE
- All fn fetch_query(), grpc_new_query(), mongoose_connect_query(), kafka_send_query(), http_client_method_query(), constructor_call_query()
- Any function named detect_connections(), detect_http_clients(), detect_mq_clients(), detect_db_clients(), detect_grpc_clients() or similar
- All ConnectionInfo struct constructions
- The `use crate::types::ConnectionInfo;` import if ConnectionInfo is no longer used

In extract(): change `connections: detect_connections(...)` (or however it is called) to `connections: vec![]`

KEEP all Express/NestJS/Next.js/Fastify route detection code intact.

Python plugin (src/plugin/lang/python.rs):

Apply same approach:
- Remove all HTTP client detection (httpx, requests, aiohttp)
- Remove all MQ detection (pika, kafka-python, celery)
- Remove all DB detection (psycopg2, sqlalchemy, pymongo, redis)
- Remove all gRPC detection (grpc.insecure_channel, etc.)
- Remove all industrial protocol detection (pymodbus, opcua, bacpypes)
- Remove associated const query strings, OnceLock caches, helper functions
- Set connections: vec![] in extract() return

KEEP FastAPI/Django/Flask decorator route detection intact.

After editing each file, verify it still compiles by checking that all remaining use statements are actually used and all OnceLock caches for route queries are still present.
</action>
  <verify>
    <automated>cargo build 2>&1 | grep -E "^error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` produces zero errors after stripping both files
    - TypeScript extract() returns connections: vec![] (grep confirms: `grep -n "connections:" src/plugin/lang/typescript.rs | grep -v "//")
    - Python extract() returns connections: vec![]
    - No `ConnectionInfo` struct construction remains in either file
    - Route detection code (Express, NestJS, FastAPI, Flask, Django) is unchanged
  </acceptance_criteria>
  <done>TypeScript and Python plugins compile with connections stripped, routes intact.</done>
</task>

<task type="auto">
  <name>Task 2: Strip connection detection from Go, Java, C#, Rust, and Ruby plugins</name>
  <files>
    src/plugin/lang/go.rs,
    src/plugin/lang/java.rs,
    src/plugin/lang/csharp.rs,
    src/plugin/lang/rust_lang.rs,
    src/plugin/lang/ruby.rs
  </files>
  <read_first>
    - src/plugin/lang/go.rs — read before editing (1098 lines)
    - src/plugin/lang/java.rs — read before editing (1142 lines)
    - src/plugin/lang/csharp.rs — read before editing (1152 lines)
    - src/plugin/lang/rust_lang.rs — read before editing (638 lines)
    - src/plugin/lang/ruby.rs — read before editing (821 lines)
  </read_first>
  <action>
Apply the same stripping pattern to each file:

Go plugin (go.rs):
- REMOVE: http.Get/http.Post/http.NewRequest detection, grpc.Dial detection, NATS client detection, DB client detection
- KEEP: net/http ServeMux handler registration, Gin route registration, Echo route registration, Fiber route registration

Java plugin (java.rs):
- REMOVE: RestTemplate/WebClient/FeignClient HTTP client detection, Kafka/RabbitMQ producer detection, gRPC newBlockingStub detection, JPA/JDBC detection
- KEEP: Spring Boot @RestController + @RequestMapping two-phase prefix joining, @GetMapping/@PostMapping/@PutMapping/@DeleteMapping/@PatchMapping detection

C# plugin (csharp.rs):
- REMOVE: HttpClient.GetAsync/PostAsync/SendAsync detection, EF Core detection, MassTransit/RabbitMQ detection, gRPC channel detection
- KEEP: ASP.NET Core [ApiController] + [HttpGet]/[HttpPost] etc. two-phase extraction, [controller] token expansion, Minimal API MapGet/MapPost/MapPut/MapDelete line scanning

Rust plugin (rust_lang.rs):
- REMOVE: reqwest::get/Client::new detection, tonic::transport::Channel::connect detection, tokio-modbus detection
- KEEP: Actix-web #[get]/#[post] macro detection, Axum Router::new().route() detection, Rocket #[get]/#[post] macro detection

Ruby plugin (ruby.rs):
- REMOVE: Faraday.new/connection detection, HTTParty HTTP client detection, Net::HTTP detection, Sidekiq::Worker detection, ActiveRecord connection detection, gRPC stub detection
- KEEP: Rails routes.rb `resources :name` expansion (→ 7 RESTful routes), Sinatra get/post/put/delete/patch route detection

For each file:
1. Remove connection-related const query strings
2. Remove connection-related OnceLock static caches
3. Remove connection-related fn helpers
4. Remove ConnectionInfo imports if no longer used
5. Set `connections: vec![]` in extract() return value

After all 5 files are edited, run `cargo test` to confirm all remaining route-detection tests pass.
</action>
  <verify>
    <automated>cargo test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` passes with zero test failures
    - `cargo build` produces zero errors
    - `grep -rn "ConnectionInfo {" src/plugin/lang/` returns zero results (no ConnectionInfo constructions in plugins)
    - `grep -rn "connections: vec!\[\]" src/plugin/lang/` returns 7 results (one per plugin)
    - `cargo clippy -- -D warnings` passes
  </acceptance_criteria>
  <done>All 7 language plugins compile with connections: vec![], route detection intact, cargo test green.</done>
</task>

</tasks>

<verification>
```bash
cargo test 2>&1 | tail -5
cargo clippy -- -D warnings 2>&1 | grep "^error"
grep -rn "ConnectionInfo {" src/plugin/lang/
grep -c "connections: vec!\[\]" src/plugin/lang/*.rs
```

Expected: 0 ConnectionInfo constructions in plugin/lang/, 7 files with connections: vec![].
</verification>

<success_criteria>
- All 7 language plugin files modified with connection detection stripped
- `grep -rn "ConnectionInfo {" src/plugin/lang/` returns zero results
- `cargo test` passes with all route-extraction tests still green
- `cargo clippy -- -D warnings` clean
- Each plugin's extract() returns `connections: vec![]`
</success_criteria>

<output>
After completion, create `.planning/phases/05-pattern-engine/05-03-SUMMARY.md`
</output>
